name: 'Changelog'
description: 'copy commit messages from git log to the file'
outputs:
  changelog:
    description: 'changelog txt file for release description (release-body)'
runs:
  using: 'docker'
  image: 'Dockerfile'
