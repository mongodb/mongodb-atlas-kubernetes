apiVersion: atlas.mongodb.com/v1
kind: AtlasDeployment
metadata:
  name: atlas-deployment-flex
spec:
  projectRef:
    name: my-project
  flexSpec:
    name: flex-instance
    providerSettings:
      backingProviderName: AWS
      regionName: US_EAST_1
