apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: mongodb-atlas-kubernetes-local
spec:
  sourceType: grpc
  image: quay.io/mongodb/mongodb-atlas-operator-catalog:0.6.0-test-1
  displayName: MongoDB Atlas operator
  publisher: MongoDB
  updateStrategy:
    registryPoll:
      interval: 10m
