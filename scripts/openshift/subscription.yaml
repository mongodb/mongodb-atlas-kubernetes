apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: mongodb-atlas-operator
  namespace: mongodb
spec:
  channel: preview
  name: mongodb-atlas-kubernetes
  source: mongodb-atlas-kubernetes
  sourceNamespace: mongodb
  installPlanApproval: Automatic
